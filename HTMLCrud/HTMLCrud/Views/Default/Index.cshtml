
@{
    ViewBag.Title = "Index";
    var emplist = ViewBag.data as List<HTMLCrud.EDM.tblemployee>;
}

<h2>Index</h2>
<p>
    <a href="@Url.Action("Create")">Create New</a>
</p>

<table class="table">
    <tr>
        <th>Employee ID</th>
        <th>First Name</th>
        <th>Email</th>
        <th>Salary</th>
        <th>Mobile</th>
        <th>Gender</th>
    </tr>
    @foreach (var item in emplist)
    {
        <tr>
            <td>@item.emp_id</td>
            <td>@item.f_name</td>
            <td>@item.email</td>
            <td>@item.salary</td>
            <td>@item.mobile</td>
            <td>@item.gender</td>
            <td><a href="/Default/Edit/@item.emp_id">Edit</a></td>
            <td><a href="@Url.Action("Delete",new { id=item.emp_id })">Delete</a></td>
        </tr>
    }
</table>